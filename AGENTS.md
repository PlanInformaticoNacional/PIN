# Agentes

## Identidad

Toda acción de Git y de GitHub en este repositorio se hace como **PlanInformaticoNacional**.

- Cuenta: `PlanInformaticoNacional`
- Repositorio: `PlanInformaticoNacional/PIN`
- Antes de usar `gh` o de publicar, la cuenta activa tiene que ser esa. Si no lo es: `gh auth switch --user PlanInformaticoNacional`
- Autor y committer: `PlanInformaticoNacional`
- No cambiar la configuración global de git. Pasar autor y committer por el entorno en cada commit.
- No hacer commits, pushes, pull requests ni comentarios con otra cuenta.
- No nombrar ninguna otra cuenta de GitHub en commits, pull requests, issues ni archivos de este repositorio.

## Historial

El historial de este repositorio solo nombra a **PlanInformaticoNacional**.

- Autor y committer de cada commit propio: `PlanInformaticoNacional`.
- No dejar otra cuenta en el autor, el committer, el mensaje ni un trailer.
- Un commit hecho en este repositorio, también desde la interfaz web, se publica con esa identidad.
- Excepción: un aporte externo que entra por pull request desde un fork. En ese caso se conserva el autor del fork. No se reescribe su identidad.
